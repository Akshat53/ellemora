import Constants from "src/types/constants.types";

const ServerConstants: Constants = {
    sortBy: 'createdAt',
    orderByDesc: -1 as -1,
    orderByAsc: 1 as 1,
};

export default ServerConstants;